<h2>Recently Added Books</h2>
<div>
    <% books.forEach(book => { %>
    <a href="books/<%=book.id%>"><img class="recentimage" height="150" width="100" src="<%= book.coverImagePath %>"></a>
    <% }) %>
</div>